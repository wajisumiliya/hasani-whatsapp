'use client';import {useState} from 'react';import {getBrowserClient} from '@/lib/supabase-browser';
export default function Login(){const [error,setError]=useState('');async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');const fd=new FormData(e.currentTarget);const {error}=await getBrowserClient().auth.signInWithPassword({email:String(fd.get('email')),password:String(fd.get('password'))});if(error)setError(error.message);else location.href='/customers'}return <main><section className="hero" style={{maxWidth:520,margin:'10vh auto'}}><div className="brand"><b>hasani</b><strong>BOOKS</strong></div><p className="eyebrow">WHATSAPP CAMPAIGN</p><h1>Admin sign in</h1><form onSubmit={login} className="stack"><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required/><button>Sign in</button></form>{error&&<div className="notice">{error}</div>}</section></main>}
