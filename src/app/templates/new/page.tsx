'use client';import {useState} from 'react';import Nav from '@/components/Nav';
export default function NewTemplate(){const [msg,setMsg]=useState(''),[result,setResult]=useState('');async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/templates',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});const j=await r.json();setResult(r.ok?'Template saved':j.error||'Unable to save')}return <main><Nav/><section className="hero compact"><p className="eyebrow">NEW TEMPLATE</p><h1>Create message template</h1><form onSubmit={save} className="form"><label>Template name<input name="template_name" required pattern="[a-z0-9_]+" placeholder="september_offer"/></label><label>Category<select name="category"><option value="marketing">Marketing</option><option value="utility">Utility</option></select></label><label>Language<input name="language_code" defaultValue="en" required/></label><label>Message<textarea name="message_body" rows={7} required onChange={e=>setMsg(e.target.value)}/></label><div className="preview"><b>Preview</b><p>{msg||'Your message will appear here.'}</p></div><button>Save template</button>{result&&<p>{result}</p>}</form></section></main>}
