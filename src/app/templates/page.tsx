import Nav from '@/components/Nav';import {getAdminClient} from '@/lib/supabase-admin';
export const dynamic='force-dynamic';
export default async function Templates(){let rows:any[]=[];try{const {data}=await getAdminClient().from('wa_templates').select('id,template_name,category,language_code,status,message_body').order('created_at',{ascending:false});rows=data||[]}catch{}return <main><Nav/><section className="hero compact"><p className="eyebrow">TEMPLATES</p><h1>Message templates</h1><p>Keep campaign copy and provider approval status together. Live provider sending should use the provider-approved template identifier.</p><a className="primary" href="/templates/new">Create template</a></section><section className="panel"><table><thead><tr><th>Name</th><th>Category</th><th>Language</th><th>Status</th><th>Message</th></tr></thead><tbody>{rows.length?rows.map(t=><tr key={t.id}><td>{t.template_name}</td><td>{t.category}</td><td>{t.language_code}</td><td><span className="pill">{t.status}</span></td><td>{t.message_body}</td></tr>):<tr><td colSpan={5}>No templates yet.</td></tr>}</tbody></table></section></main>}
