const cards=[['Customers','Import, clean and manage opted-in customer records.'],['Templates','Prepare approved WhatsApp message templates.'],['Campaigns','Create, schedule, pause and monitor campaigns.'],['Delivery','Track queued, sent, delivered, read and failed messages.']];
export default function Home(){return <main><header><div className="brand"><b>hasani</b><strong>BOOKS</strong></div><span>WhatsApp Campaign Console</span></header><section className="hero"><p className="eyebrow">CUSTOMER COMMUNICATION</p><h1>Campaign operations, in one place.</h1><p>Import consented customers, prepare campaigns and monitor delivery from a production-oriented dashboard.</p><div className="status">Provider mode: <b>{process.env.WHATSAPP_PROVIDER||'mock'}</b></div></section><section className="grid">{cards.map(([t,d])=><article key={t}><h2>{t}</h2><p>{d}</p><button type="button">Open {t}</button></article>)}</section><section className="notice"><b>Sending protection</b><p>Campaign recipients must have active WhatsApp consent and must not be present in the opt-out register. Provider sending remains disabled while mock mode is configured.</p></section></main>}
