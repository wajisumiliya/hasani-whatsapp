import {NextResponse} from 'next/server';import {getAdminClient} from '@/lib/supabase-admin';import {authError,requireAdmin} from '@/lib/require-admin';import {analyzeCustomerFile} from '@/lib/customer-import';export const runtime='nodejs';
export async function POST(req:Request){try{await requireAdmin(req)}catch(e){const a=authError(e);return NextResponse.json({error:a.error},{status:a.status})}const form=await req.formData(),file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'Excel or CSV file is required'},{status:400});if(file.size>15*1024*1024)return NextResponse.json({error:'File exceeds 15MB limit'},{status:413});let a;try{a=await analyzeCustomerFile(file)}catch{return NextResponse.json({error:'Unable to read Excel/CSV file'},{status:400})}const db=getAdminClient();if(a.valid.length){const {error}=await db.from('wa_customers').upsert(a.valid,{onConflict:'phone',ignoreDuplicates:true});if(error)return NextResponse.json({error:error.message},{status:500})}return NextResponse.json({rows:a.rows,ready:a.valid.length,rejected:a.errors.length,duplicates:a.duplicates,invalid:a.invalid,noConsent:a.noConsent,errors:a.errors.slice(0,500)})}
