import {NextResponse} from 'next/server';import {authError,requireAdmin} from '@/lib/require-admin';import {analyzeCustomerFile} from '@/lib/customer-import';export const runtime='nodejs';
export async function POST(req:Request){try{await requireAdmin(req)}catch(e){const a=authError(e);return NextResponse.json({error:a.error},{status:a.status})}const form=await req.formData(),file=form.get('file');if(!(file instanceof File))return NextResponse.json({error:'Excel or CSV file is required'},{status:400});if(file.size>15*1024*1024)return NextResponse.json({error:'File exceeds 15MB limit'},{status:413});try{const a=await analyzeCustomerFile(file);return NextResponse.json({rows:a.rows,ready:a.valid.length,rejected:a.errors.length,duplicates:a.duplicates,invalid:a.invalid,noConsent:a.noConsent,preview:a.valid.slice(0,50).map(({customer_code,customer_name,phone,email})=>({customer_code,customer_name,phone,email})),errors:a.errors.slice(0,100)})}catch{return NextResponse.json({error:'Unable to read Excel/CSV file'},{status:400})}}
